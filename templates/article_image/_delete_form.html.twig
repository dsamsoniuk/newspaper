<form method="post" action="{{ path('app_article_image_delete', {'id': article_image.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article_image.id) }}">
    <button class="btn">Delete</button>
</form>
